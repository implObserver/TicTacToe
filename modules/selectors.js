const Selectors = (() => {
    const iconMenu = document.querySelector('.select');
    const wrapper = document.querySelector('.wrapper');
    const menu = document.querySelector('.menu');
    return { iconMenu, wrapper, menu };
})();

export { Selectors };